// {{name}} component client side namespace
export const {{name}} = (el) => {
  // Object to house all common selected elements within a components context
  const ui = {
    el
  };

  // {{name}} component client side main init point
  const init = () => {
    // {{name}} init logic here
  };

  // Self init point
  init();
};

export default {{name}};
