// {{name}} component client side namespace
export const {{name}} = (el) => {
  // {{name}}'s helper UI object
  const ui = {
    el
  };

  // {{name}}'s main init point
  const init = () => {
    // {{name}} init logic here
  };

  // Self init
  init();
};

export default {{name}};
