// Style guide config
import ExampleBG from '@guide/assets/example_bg.png';

const config = {
  example: {
    options: {
      background: ExampleBG,
      padding: '1rem',
      brightness: 1
    }
  }
};

export default config;
